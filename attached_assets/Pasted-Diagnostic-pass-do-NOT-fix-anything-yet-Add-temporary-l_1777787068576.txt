Diagnostic pass — do NOT fix anything yet. Add temporary logging, report findings, then I'll send the fix prompt.

Two issues to diagnose:

ISSUE 1: Photoreal 3D Tiles render with a hard vertical seam at low altitude. Half the view shows photoreal terrain, the other half shows flat ellipsoid blue. Network panel shows tile requests are 200ing fine (with normal Cesium pan-cancel behavior). So this is NOT a fetch problem — it's a render/LOD/cache problem.

Add to Viewer.tsx (or wherever the Cesium viewer is initialized):

1. Enable Cesium's debug stats:
   viewer.scene.debugShowFramesPerSecond = true;

2. Log the Google 3D Tiles tileset config on mount. Find the Cesium3DTileset instance for Google Photoreal and console.log:
   - tileset.maximumScreenSpaceError (current value)
   - tileset.maximumMemoryUsage
   - tileset.cacheBytes (if available in this Cesium version)
   - tileset.dynamicScreenSpaceError
   - tileset.dynamicScreenSpaceErrorDensity
   - tileset.dynamicScreenSpaceErrorFactor
   - tileset.preloadWhenHidden
   - tileset.preloadFlightDestinations

3. Add a tileset event listener for tile load failures:
   tileset.tileFailed.addEventListener((error) => {
     console.warn('[TILE FAIL]', error.url, error.message);
   });

4. Every 5 seconds while running, log:
   - tileset.statistics.numberOfTilesWithContentReady
   - tileset.statistics.numberOfPendingRequests
   - tileset.statistics.numberOfAttemptedRequests
   - viewer.camera.positionCartographic.height (camera altitude in meters)
   - current FPS (from Cesium debug stat)

ISSUE 2: Vessels show fresh telemetry in the side panel (e.g. 13.3 kts, course 031°) but the icon does not move across the globe. Either AISStream isn't pushing position updates for clicked MMSIs, or the VesselLayer Entity position is not reacting to store changes.

Add to VesselLayer.ts:

5. When a vessel is clicked / set as selectedEntity, log every 10 seconds for that MMSI:
   - timestamp of last store update for that vessel (whatever field tracks lastUpdate / lastSeen)
   - current lat/lon from store
   - current lat/lon being rendered on the Cesium Entity's position
   
   Format: `[VESSEL TICK] MMSI=X storeUpdate=<ts> storeLat=<lat> storeLon=<lon> entityLat=<lat> entityLon=<lon>`

6. In the AISStream WebSocket handler, log when ANY position update arrives:
   `[AIS POSITION] MMSI=X lat=<lat> lon=<lon>`
   (just for the first 60 seconds after page load, then stop, to avoid log spam)

After adding all logging, restart the dev server and let it run for 90 seconds. Then paste back to me:
- The Cesium tileset config dump
- 3-4 samples of the 5-second tile/altitude/FPS logs (especially when the seam is visible)
- 3-4 samples of the [VESSEL TICK] logs for one selected vessel
- A summary of how many [AIS POSITION] events fired in 60 seconds, and how many unique MMSIs

Do not change tileset settings, do not change vessel rendering logic, do not modify trail rendering. Logging only.
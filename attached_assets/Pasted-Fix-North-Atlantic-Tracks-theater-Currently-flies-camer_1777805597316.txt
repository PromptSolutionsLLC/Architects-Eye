Fix North Atlantic Tracks theater. Currently flies camera to empty mid-Atlantic (-40°, 50°, 3500km altitude, pitch -55°) where there's nothing visible — just empty ocean.

Goal: theater should showcase transatlantic flight corridors (the famous "NATs" — North Atlantic Tracks routing system) where commercial aviation crosses between North America and Europe.

Update theater config in theaters.ts (or wherever the 6 theater definitions live):

CHANGE the natlantic theater entry FROM:
- lon: -40.0, lat: 50.0, height: 3500000, pitch: -55°

TO:
- lon: -45.0
- lat: 52.0
- height: 1500000 (1500km — closer in to actually see aircraft)
- pitch: Cesium.Math.toRadians(-50)
- heading: 0 (looking north toward Greenland)

This frames the camera over the central NAT corridor between Newfoundland and Ireland — the densest transatlantic flight zone — at an altitude where individual aircraft become visible.

Additionally, when the natlantic theater is activated, ensure these layers are AUTO-ENABLED (set layerVisibility before flyTo):
- aircraft: true
- vessels: true
- submarineCables: true (cables are dense in the North Atlantic — visual payoff)
- airspace: true (NAT entry/exit points have restricted airspace)

DO NOT enable: jamming, fires, quakes, satellites (already on by default at globe scale, irrelevant here)

If the theater system already has a per-theater layer-enable hook (like California Wildfire Belt has fires:true), use that pattern. If not, add it minimally — set layerVisibility for the listed layers before camera.flyTo.

flyDuration stays 4.5s (existing).

Verify after fix:
- Click "North Atlantic Tracks" theater button → camera flies to mid-Atlantic at ~1500km altitude looking north
- Visible in frame: aircraft trails crossing the ocean, submarine cables glowing across the seabed, vessels along coasts of Newfoundland/Ireland
- Frame is NOT just empty ocean and Greenland's ice cap

Do not touch the other 5 theaters, fetch logic, click handlers, or any other system.
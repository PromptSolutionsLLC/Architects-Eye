Goal: build the full UI chrome around the Cesium canvas. No new data layers yet — just the visual frame to host them.

Implementation:

1. src/components/HeaderBar.tsx — top bar, fixed position, full width, height 40px:
   - Left: "ARCHITECT'S EYE" in bold uppercase tracking-widest, with a small SVG logo (a circle with crosshairs) to its left
   - Center: Classification banner: "UNCLASSIFIED // OSINT" — amber background bg-amber-500/20, dark amber text, monospace, small padding
   - Right: live status pill showing "● LIVE" in green by default, monospace UTC clock that ticks every second

2. src/components/LayerToggles.tsx — left sidebar, fixed position, width 220px, top 40px, height calc(100vh - 100px):
   - Header: "DATA LAYERS" uppercase tracking-widest text-cyan-400 small
   - Toggle row per layer: aircraft, vessels, satellites, jamming, fires, quakes
   - Each row: colored dot (per the project's color encoding), layer name, count of currently visible entities, toggle switch on the right
   - Active layer toggles wire to a layerVisibility object in the Zustand store
   - Currently only aircraft is fully implemented — others are stubs with grayed-out toggles

3. Below LayerToggles, in the same left sidebar: src/components/TheaterPanel.tsx
   - Header: "THEATERS" uppercase tracking-widest text-cyan-400 small
   - 5 named buttons stacked vertically: "Strait of Hormuz", "Black Sea", "North Atlantic Tracks", "Korean DMZ", "California Wildfire Belt"
   - Each button is dark with a subtle border, hover brightens border to cyan
   - Each button stub-fires a function flyToTheater(name) that just console.logs for now (we wire it in P7)

4. EntityPanel.tsx — already exists. Just confirm it slides in from the right, width 320px, top 40px, only shown when selectedEntity is not null.

5. src/components/Timeline.tsx — bottom bar, fixed position, full width, height 60px:
   - Range slider from −6h (left) to "now" (right)
   - Label on left "−6h" and right "now" in monospace
   - Default at "now" (live mode)
   - Drag changes a timeOffsetMs value in the store
   - Wire the live status pill in HeaderBar to flip from green "● LIVE" to amber "● REPLAY −Xh Ym" when timeOffsetMs > 0
   - Drag end at "now" (offset 0) sets back to live mode
   - Don't actually replay aircraft yet — that's P8. Just wire the visual state machine.

6. Extend Tailwind config theme tokens:
   - Background: bg-slate-950 base, surfaces bg-slate-900/95
   - Borders: cyan-500/20 default, cyan-500/40 hover
   - Accent: cyan-400 for active, amber-400 for warnings, red-400 for alerts

Critical: Cesium canvas must remain visible behind all UI panels. Use pointer-events: none on container divs and pointer-events: auto on actual UI cards. Test by clicking a plane through the empty area of the left sidebar — should still register the click.

Definition of done: the UI chrome is fully built around the existing aircraft layer. Layer toggles flip visibility flags (only aircraft works since it's the only one wired). Theater buttons console.log. Timeline drags but doesn't replay yet. Live → REPLAY badge state machine works. All cinematic styling in place.

Do not add new data layers. UI shell only.